export function Logo({ className }: { className?: string }) {
  return (
    <span className={className}>
      <span className="font-serif text-lg tracking-tight text-text-primary">
        Aether<span className="text-accent">Impact</span>
      </span>
    </span>
  );
}
